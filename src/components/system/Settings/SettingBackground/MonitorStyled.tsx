import { Monitor } from "react95"
import styled from "styled-components"

const MonitorStyled = styled(Monitor)`
  position: relative;
  left: 50%;
  transform: translateX(-50%);
`

export default MonitorStyled
